{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "command": "${workspaceFolder}/.dotnet/dotnet.exe",
      "type": "process",
      "args": [
        "build",
        "${workspaceFolder}/VideoSite.csproj"
      ],
      "problemMatcher": "$msCompile",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
